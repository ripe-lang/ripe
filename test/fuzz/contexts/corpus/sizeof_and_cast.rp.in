func main() i32 {
  var n: i32 = 1
  var u: u32 = cast(u32, n)
  var p: *i32 = &n
  var q: *u32 = cast(*u32, p)
  var a: usize = cast(usize, q)
  if a == 0 { return 1 }
  return cast(i32, sizeof(i32)) + cast(i32, u) + *p
}
