extern "C" func printf(fmt: cstr, ...) i32

struct pt { x: i32, y: i32 }

func main() i32 {
  let p = {
    var scale = 3
    pt { x: 2 * scale, y: 5 * scale }
  }
  printf("%d %d\n", p.x, p.y)
  return 0
}
