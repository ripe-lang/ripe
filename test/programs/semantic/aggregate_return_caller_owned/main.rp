struct pair {
  x: i32
  y: i32
}

func make_pair(x: i32, y: i32) pair {
  return pair { x: x, y: y }
}

func sum_pair(left: pair, right: pair) i32 {
  return left.x + left.y + right.x + right.y
}

func main() i32 {
  return sum_pair(make_pair(1, 2), make_pair(30, 40))
}
