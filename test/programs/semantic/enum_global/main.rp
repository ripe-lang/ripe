extern "C" func printf(fmt: cstr, ...) i32

enum State { Idle; Busy }

var start: State = State.Idle
var current: State = State.Idle

func main() i32 {
  current = State.Busy
  printf("%d %d\n", start == State.Idle, current == State.Busy)
  return 0
}
