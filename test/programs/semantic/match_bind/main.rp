extern "C" func printf(fmt: cstr, ...) i32

// A bare name in a pattern binds the scrutinee and catches everything
func describe(n: i32) i32 {
  return match n {
    0 => 100
    other => other * 2

  }
}

func main() i32 {
  printf("%d %d %d\n", describe(0), describe(3), describe(-4))
  return 0
}
