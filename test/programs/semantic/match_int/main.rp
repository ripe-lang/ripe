extern "C" func printf(fmt: cstr, ...) i32

func classify(n: i32) i32 {
  return match n {
    0 => 100,
    1 => 200,
    2 => 300,
    _ => 999,
  }
}

func main() {
  printf("%d %d %d %d\n", classify(0), classify(1), classify(2), classify(7))
  return
}
