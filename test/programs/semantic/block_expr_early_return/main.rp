extern "C" func printf(fmt: cstr, ...) i32

func pick(n: i32) i32 {
  var r = {
    if n == 0 { return 0 - 1 }
    n * 10
  }
  return r
}

func main() i32 {
  printf("%d %d\n", pick(0), pick(3))
  return 0
}
