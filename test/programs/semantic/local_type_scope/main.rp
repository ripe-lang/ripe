func main() i32 {
  let first = {
    struct Value { number: i32 }
    let value = Value { number: 5 }
    value.number
  }

  let second = {
    struct Value { number: i32, extra: i32 }
    let value = Value { number: 1, extra: 7 }
    value.extra
  }

  return first + second
}
