func main() i32 {
  var first = {
    struct Value { number: i32 }
    var value = Value { number: 5 }
    value.number
  }

  var second = {
    struct Value { number: i32; extra: i32 }
    var value = Value { number: 1, extra: 7 }
    value.extra
  }

  return first + second
}
