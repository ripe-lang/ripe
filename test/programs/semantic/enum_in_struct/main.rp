extern "C" func printf(fmt: cstr, ...) i32

enum Kind { Circle; Square }

struct Shape { kind: Kind; size: i32 }

func main() {
  var s = Shape { kind: Kind.Square, size: 3 }
  printf("%d %d\n", s.kind == Kind.Square, s.size)
  return
}
