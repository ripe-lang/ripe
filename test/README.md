# Test

You can use `unit` to test each part of the compiler on its own, `fuzz` to
break working programs and look for bad error messages, and `programs` to run whole programs and check what they print against a saved copy.

```
$ dune test
```
