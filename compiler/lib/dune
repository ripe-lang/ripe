(library
 (name ripe)
 (modules ast lexer parser types typechecker typed_ast codegen)
 (libraries menhirLib)
 (preprocess
  (pps ppx_deriving.show)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser))
