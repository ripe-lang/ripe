struct point { x: i32, y: i32 }

extern add(a: i32, b: i32): i32

public add(a: i32, b: i32): i32 {
  #while a > 0 {
  #}
  #for i in 0..len {
  #}

  if a > 0 {
    if a > 1 {
    } else {
    }
  } elseif a < 0 {
  } else {
  }
}

main() {
}
