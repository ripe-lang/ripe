(library
 (name ripe)
 (modules
  span
  ast
  source_map
  diagnostic
  error
  tokens
  lexer
  parser
  types
  symbol
  resolve
  typechecker
  typed_ast
  core
  const_eval
  const_fold
  lower
  codegen
  runtime_src
  driver)
 (libraries unix)
 (preprocess
  (pps ppx_deriving.show)))

(ocamllex
 (modules lexer))

(rule
 (target runtime_src.ml)
 (deps ../runtime/panic.c)
 (action
  (with-stdout-to
   %{target}
   (progn
    (echo "let source = {rt|")
    (cat ../runtime/panic.c)
    (echo "|rt}\n")))))
