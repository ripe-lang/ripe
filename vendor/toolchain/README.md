The Linux x86 64 toolchain contains the QBE executable and the LLD files used
by the development compiler.

The QBE is built from the `vendor/qbe` submodule.

LLD is version 21.1.8 and comes from the LLVM project.

The bundled LLD needs the libraries in the matching `lib` directory.
